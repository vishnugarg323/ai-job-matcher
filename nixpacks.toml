[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip', 'python311Packages.setuptools', 'chromium', 'chromedriver']

[phases.install]
cmds = ['python -m pip install --upgrade pip --break-system-packages', 'python -m pip install -r requirements.txt --break-system-packages']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'python web_app.py'
