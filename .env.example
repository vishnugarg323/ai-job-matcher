# Google Gemini API (Required - FREE)
GEMINI_API_KEY=your-gemini-api-key-here
# Get free key: https://makersuite.google.com/app/apikey

# Flask Secret Key (Required)
SECRET_KEY=your-random-secret-key-change-in-production

# Email Configuration (OPTIONAL - for email notifications)
# If not set, jobs will only be shown in dashboard (still works!)
# EMAIL_SENDER=your_email@gmail.com
# EMAIL_PASSWORD=your_gmail_app_password
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587

# LinkedIn Credentials (OPTIONAL - for LinkedIn job scraping)
# If not set, only Indeed + StepStone will be used (still plenty of jobs!)
# Note: Same LinkedIn account used for all profiles (scraping only)
# LINKEDIN_EMAIL=your_linkedin_email@example.com
# LINKEDIN_PASSWORD=your_linkedin_password

# Database Configuration
DATABASE_PATH=data/jobs.db

# Application Settings
LOG_LEVEL=INFO
PORT=5000
FLASK_ENV=production

# Scraping Configuration
MAX_PAGES_PER_SITE=5
REQUEST_DELAY=2
USER_AGENT_ROTATION=true
