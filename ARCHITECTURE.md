# 🏗️ System Architecture - AI Job Matcher

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     AI JOB MATCHER                          │
│                   (Main Application)                         │
└─────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
    ┌───────────┐   ┌───────────┐   ┌───────────┐
    │  Scraping │   │  Matching │   │Scheduling │
    │   Layer   │   │   Layer   │   │   Layer   │
    └───────────┘   └───────────┘   └───────────┘
            │               │               │
            └───────────────┴───────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   Database    │
                    │    (SQLite)   │
                    └───────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ Notifications │
                    │    (Email)    │
                    └───────────────┘
```

## Detailed Component Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
├──────────────────────────────────────────────────────────────────┤
│  • Command Line Interface (CLI)                                  │
│  • Email Notifications (HTML)                                    │
│  • Database Viewer (SQL)                                         │
│  • Logs (app.log)                                               │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                      APPLICATION CORE                            │
├──────────────────────────────────────────────────────────────────┤
│  main.py                                                         │
│  ├─ JobMatcherApp                                               │
│  │  ├─ Initialize components                                    │
│  │  ├─ Orchestrate workflow                                     │
│  │  └─ Handle errors                                            │
│  └─ Scheduler integration                                        │
└──────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  SCRAPERS   │      │  MATCHERS   │      │  DATABASE   │
├─────────────┤      ├─────────────┤      ├─────────────┤
│ Base        │      │ Resume      │      │ Connection  │
│ ├─ Indeed   │      │ Parser      │      │ Management  │
│ ├─ StepStone│      │             │      │             │
│ └─ LinkedIn │      │ Job         │      │ CRUD Ops    │
│             │      │ Matcher     │      │             │
│ Features:   │      │             │      │ Dedup       │
│ • Selenium  │      │ ATS         │      │             │
│ • Parsing   │      │ Analyzer    │      │ Queries     │
│ • Anti-block│      │             │      │             │
└─────────────┘      └─────────────┘      └─────────────┘
                              │
                     ┌────────┴────────┐
                     ▼                 ▼
            ┌─────────────┐    ┌─────────────┐
            │  NOTIFIERS  │    │  SCHEDULER  │
            ├─────────────┤    ├─────────────┤
            │ Email       │    │ APScheduler │
            │ ├─ HTML     │    │             │
            │ ├─ SMTP     │    │ Cron        │
            │ └─ Template │    │ Triggers    │
            └─────────────┘    └─────────────┘
```

## Data Flow Diagram

```
START
  │
  ├─► Load Configuration (config.yaml + .env)
  │
  ├─► Parse Resume (data/resume.txt)
  │     │
  │     └─► Extract: Skills, Experience, Education
  │           │
  │           └─► Generate AI Embedding (OpenAI)
  │
  ├─► Scrape Jobs (Parallel)
  │     │
  │     ├─► Indeed.de ──┐
  │     ├─► StepStone ──┼─► Job Listings (Title, Company, Desc, URL)
  │     └─► LinkedIn ───┘
  │
  ├─► Filter Duplicates (Database Check)
  │     │
  │     └─► Hash: MD5(title + company + location)
  │
  ├─► Match Jobs (AI + Keywords)
  │     │
  │     ├─► For each job:
  │     │     │
  │     │     ├─► Generate Job Embedding (OpenAI)
  │     │     │
  │     │     ├─► Calculate Cosine Similarity
  │     │     │     └─► AI Score (0-1)
  │     │     │
  │     │     ├─► Extract Job Keywords
  │     │     │
  │     │     ├─► Match with Resume Keywords
  │     │     │     └─► Keyword Score (0-1)
  │     │     │
  │     │     ├─► Weighted Average
  │     │     │     └─► Final Score = 
  │     │     │         (AI × 0.15) + 
  │     │     │         (Keywords × 0.40) + 
  │     │     │         (Experience × 0.30) +
  │     │     │         (Education × 0.15)
  │     │     │
  │     │     └─► Filter: score >= 0.90
  │     │
  │     └─► Matched Jobs (sorted by score)
  │
  ├─► Save to Database (SQLite)
  │     │
  │     └─► Table: jobs
  │           ├─ id, job_hash, title
  │           ├─ company, location, url
  │           ├─ description, salary
  │           ├─ match_score, keywords
  │           └─ created_at, notified
  │
  ├─► Generate Email Report
  │     │
  │     ├─► HTML Template
  │     │     ├─ Header: "X New Job Matches"
  │     │     ├─ For each job:
  │     │     │   ├─ Title, Company, Location
  │     │     │   ├─ Match Score (badge)
  │     │     │   ├─ Salary (if available)
  │     │     │   ├─ Description snippet
  │     │     │   ├─ Matched keywords
  │     │     │   └─ Apply button (link)
  │     │     └─ Footer
  │     │
  │     └─► Send via SMTP
  │           └─► Gmail (TLS)
  │
  └─► Schedule Next Run (if scheduled mode)
        │
        └─► Wait until next run time (e.g., 8 AM tomorrow)
              │
              └─► REPEAT
```

## Component Interactions

```
┌────────────┐
│   Config   │ (config.yaml + .env)
│   Loader   │
└─────┬──────┘
      │
      ├──► ResumeParser ──────┐
      │                       │
      ├──► IndeedScraper ─────┤
      ├──► StepStoneScraper ──┤
      ├──► LinkedInScraper ───┤
      │                       │
      ├──► JobMatcher ────────┤
      │      │                │
      │      └─► OpenAI API   │
      │                       │
      ├──► ATSAnalyzer ───────┤
      │                       │
      ├──► DatabaseManager ───┤
      │      │                │
      │      └─► SQLite       │
      │                       │
      ├──► EmailNotifier ─────┤
      │      │                │
      │      └─► SMTP Server  │
      │                       │
      └──► JobScheduler ──────┘
             │
             └─► APScheduler
```

## Technology Stack Layers

```
┌──────────────────────────────────────────────┐
│          PRESENTATION LAYER                  │
│  • CLI (argparse)                           │
│  • Email (HTML/CSS)                         │
│  • Logs (colorlog)                          │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│          APPLICATION LAYER                   │
│  • Python 3.9+                              │
│  • Main Application Logic                   │
│  • Workflow Orchestration                   │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│          BUSINESS LOGIC LAYER                │
│  • Resume Parsing                           │
│  • Job Scraping                             │
│  • AI Matching                              │
│  • ATS Analysis                             │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│          INTEGRATION LAYER                   │
│  • OpenAI API (embeddings)                  │
│  • Selenium WebDriver                       │
│  • SMTP Protocol                            │
│  • APScheduler                              │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│          DATA LAYER                          │
│  • SQLite Database                          │
│  • File System (resume, config, logs)      │
│  • Environment Variables (.env)             │
└──────────────────────────────────────────────┘
```

## External Dependencies

```
┌─────────────────────────────────────────────────┐
│              EXTERNAL SERVICES                  │
└─────────────────────────────────────────────────┘
                      │
      ┌───────────────┼───────────────┬────────────────┐
      │               │               │                │
┌──────────┐  ┌───────────┐  ┌────────────┐  ┌────────────┐
│ OpenAI   │  │  Indeed   │  │ StepStone  │  │  LinkedIn  │
│   API    │  │   .de     │  │    .de     │  │            │
└──────────┘  └───────────┘  └────────────┘  └────────────┘
      │               │               │                │
   Embeddings    Job Posts      Job Posts        Job Posts
   (Cost: ~$0.01/day)  (Free)        (Free)           (Free)
```

## Security Architecture

```
┌────────────────────────────────────────┐
│         SECURITY LAYERS                │
├────────────────────────────────────────┤
│ 1. Environment Variables (.env)        │
│    • Not in version control            │
│    • API keys, passwords               │
│    • gitignore protection              │
├────────────────────────────────────────┤
│ 2. API Key Encryption                  │
│    • HTTPS/TLS for OpenAI API          │
│    • SMTP TLS for emails               │
├────────────────────────────────────────┤
│ 3. Local Data Storage                  │
│    • SQLite (no remote access)         │
│    • Resume stays on local machine     │
├────────────────────────────────────────┤
│ 4. Anti-Scraping Measures              │
│    • Rate limiting (2s delay)          │
│    • User agent rotation               │
│    • Headless browser mode             │
│    • Respect robots.txt                │
└────────────────────────────────────────┘
```

## Error Handling Flow

```
TRY:
  │
  ├─► Parse Resume
  │     └─► CATCH: FileNotFoundError → Notify user
  │
  ├─► Scrape Jobs
  │     └─► CATCH: TimeoutException → Skip page, continue
  │
  ├─► Call OpenAI API
  │     └─► CATCH: APIError → Log error, retry (max 3)
  │
  ├─► Match Jobs
  │     └─► CATCH: ValueError → Skip job, continue
  │
  ├─► Save to Database
  │     └─► CATCH: IntegrityError → Skip duplicate
  │
  └─► Send Email
        └─► CATCH: SMTPException → Log error, continue

FINALLY:
  └─► Close connections, cleanup resources
```

## Scalability Considerations

```
┌────────────────────────────────────────┐
│      CURRENT (Single User)             │
├────────────────────────────────────────┤
│ • Local SQLite database                │
│ • Sequential scraping                  │
│ • 100-200 jobs per run                 │
│ • ~5-10 min execution time             │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│      FUTURE ENHANCEMENTS               │
├────────────────────────────────────────┤
│ • PostgreSQL for multi-user            │
│ • Parallel scraping (ThreadPool)       │
│ • Caching layer (Redis)                │
│ • API server (FastAPI)                 │
│ • Queue system (Celery)                │
│ • Container deployment (Docker)        │
└────────────────────────────────────────┘
```

## Performance Optimization Points

```
1. CACHING
   ├─ Resume embedding (cached after first parse)
   └─ Job descriptions (avoid re-scraping)

2. PARALLEL PROCESSING
   ├─ Scrape multiple portals simultaneously
   └─ Batch OpenAI API calls

3. DATABASE INDEXING
   ├─ Index on job_hash (duplicate detection)
   ├─ Index on match_score (queries)
   └─ Index on created_at (cleanup)

4. RATE LIMITING
   ├─ Respect API limits (OpenAI: 3,000 RPM)
   └─ Delay between scraper requests (2s)

5. RESOURCE CLEANUP
   ├─ Close Selenium drivers
   ├─ Close database connections
   └─ Rotate log files
```

## Monitoring & Observability

```
┌────────────────────────────────────────┐
│            LOGGING                     │
├────────────────────────────────────────┤
│ • Console output (colored)             │
│ • File logging (app.log)               │
│ • Rotation (10MB, 5 backups)           │
│ • Levels: DEBUG, INFO, WARNING, ERROR  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│            METRICS                     │
├────────────────────────────────────────┤
│ • Jobs scraped per portal              │
│ • Match scores distribution            │
│ • API call counts                      │
│ • Execution time                       │
│ • Error rates                          │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│            ALERTS                      │
├────────────────────────────────────────┤
│ • Email if no matches found            │
│ • Log critical errors                  │
│ • Track API quota usage                │
└────────────────────────────────────────┘
```

---

This architecture is designed for:
- ✅ Reliability (error handling at every layer)
- ✅ Maintainability (modular components)
- ✅ Scalability (can extend to multi-user)
- ✅ Security (local data, encrypted APIs)
- ✅ Performance (caching, parallel processing)
